import 'package:flutter/material.dart';

class TeacherUploadNotesScreen extends StatelessWidget {
  const TeacherUploadNotesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Upload Notes')),
      body: const Center(
        child: Text('Upload feature coming soon!', style: TextStyle(fontSize: 18)),
      ),
    );
  }
}
